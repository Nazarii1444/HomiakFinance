services:
  nginx:
    image: nginx:alpine
    container_name: homiak-nginx
    ports:
      - "8000:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    networks:
      - homiak-net

networks:
  homiak-net:
    external: true

