# 🖥️ Homiak Finance — UI Тестування (UI Test Cases)

**Проєкт:** Homiak Finance Frontend  
**Дата:** 14.04.2026  
**Середовище:** React + Redux Toolkit · MUI (Material UI) · Chrome 124+ · Desktop (1920×1080)  
**Інструмент:** Ручне тестування у браузері

> Усього тест-кейсів: **20**  
> Категорії: роутинг, дашборд, транзакції, цілі, профіль, адаптивність, UX

---

## 1 · Роутинг та навігація

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-01 | Редирект `/` → `/dashboard` | Авторизований користувач | 1. Відкрити `http://localhost:5173/` | Автоматичний редирект на `/dashboard`; відображається компонент `Dashboard` | Редирект працює, dashboard відкривається | 🔴 High |
| UI-02 | Захищений роут без токена | Redux store не містить `accessToken`; `persistor` очищено | 1. Відкрити `http://localhost:5173/dashboard` без авторизації | `ProtectedLayout` перенаправляє на `/login`; сторінка Dashboard НЕ відображається | Редирект на логін, доступ обмежено | 🔴 High |
| UI-03 | `PublicRoute` редирект для авторизованого | Користувач авторизований (`accessToken` є в Redux store) | 1. Відкрити `http://localhost:5173/login`<br>2. Відкрити `http://localhost:5173/register` | Обидва маршрути перенаправляють на `/dashboard` через `PublicRoute`; форми логіну/реєстрації НЕ відображаються | Редирект працює, форми не видно | 🟠 High |
| UI-04 | Невідомий маршрут → `/dashboard` | Авторизований користувач | 1. Відкрити `http://localhost:5173/some-unknown-page` | Відповідно до `<Route path="*">` — редирект на `/dashboard` | Редирект на dashboard виконується | 🟡 Medium |

---

## 2 · PersistGate — завантаження стану

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-05 | Loading-стан під час гідратації store | Повільний пристрій або великий `persistedState` у `localStorage` | 1. Очистити кеш браузера<br>2. Відкрити додаток | Компонент `Loading` (повноекранний `CircularProgress`) відображається до завершення гідратації `PersistGate`; після завершення — відображається цільова сторінка | Loader показується і зникає після завантаження | 🟡 Medium |

---

## 3 · Дашборд — Balance Card

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-06 | Відображення `Total Balance` з символом валюти | Авторизований, `default_currency`=`"UAH"`, `totalBalance`=`15000.50` | 1. Відкрити `/dashboard` | У блоці `balance-card` відображається `15,000.50₴`; підписи `Income` та `Expenses` зі значеннями через `toLocaleString('en-US')` | Баланс і формат відображаються правильно | 🔴 High |
| UI-07 | Від'ємний `Total Balance` | Авторизований, витрати перевищують доходи (`totalBalance`=`-200.00`) | 1. Відкрити `/dashboard` | Відображається `-200.00$` (або відповідний символ валюти); знак мінус виводиться через умову `totalBalance < 0 ? '-' : ''` | Від’ємний баланс показується коректно | 🟠 High |
| UI-08 | Empty state при відсутності транзакцій | Авторизований, `transactions.length`=`0`, завантаження завершено (`loading`=`false`) | 1. Відкрити `/dashboard`<br>2. Дочекатись завершення запиту | Відображається заглушка з текстом `"No transactions found"` та `"Start by adding your first transaction"`; список `<List>` НЕ рендериться | Empty state показується коректно | 🟠 High |
| UI-09 | `CircularProgress` під час завантаження транзакцій | `loading`=`true`, `transactions.length`=`0` (перший запит) | 1. Відкрити `/dashboard` до завершення `fetchTransactions` | Замість дашборду відображається `<CircularProgress>` по центру екрану (`height: 50vh`); після завершення запиту — дашборд відображається | Loader працює, після — контент | 🟠 High |

---

## 4 · Дашборд — список транзакцій

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-10 | Форматування суми: income vs expense | Авторизований; дві транзакції — income `100 USD` та expense `50 PLN` | 1. Відкрити `/dashboard`<br>2. Знайти обидві транзакції у списку | Income: `+$100.00`; Expense PLN: `-50.00 zł` (символ після суми для PLN, SEK, NOK) | Формат сум відображається правильно | 🟠 High |
| UI-11 | Іконка категорії | Авторизований; наявні транзакції з `category_name`: `"food"`, `"travel"`, `"unknownCategory"` | 1. Відкрити `/dashboard`<br>2. Знайти відповідні транзакції | `food` → `<RestaurantIcon>`; `travel` → `<FlightIcon>`; `unknownCategory` → fallback `<AttachMoneyIcon>` (з `categoryIcons.default`) | Іконки відображаються коректно | 🟡 Medium |
| UI-12 | Кнопка Delete — видимість при hover | Авторизований, є транзакції | 1. Навести курсор на рядок транзакції<br>2. Прибрати курсор | Крок 1: `DeleteIcon` стає видимим (`opacity: 1`); крок 2: іконка знову прихована (`opacity: 0`, `transition: 0.2s`) | Hover працює, кнопка з’являється | 🟠 High |
| UI-13 | Діалог підтвердження видалення | Авторизований; транзакція з `category_name`=`"food"` існує | 1. Навести курсор на транзакцію `"food"`<br>2. Клікнути `DeleteIcon`<br>3. Перевірити вміст діалогу | Відкривається `<Dialog>` з заголовком `"Delete Transaction"` та текстом підтвердження; є кнопки `Cancel` та `Delete` | Діалог відкривається з правильним текстом | 🔴 High |
| UI-14 | Відміна видалення через `Cancel` | Діалог підтвердження відкрито | 1. Натиснути кнопку `Cancel` (outlined) | Діалог закривається; транзакція залишається у списку | Відміна працює, дані не змінюються | 🔴 High |
| UI-15 | Підтвердження видалення через `Delete` | Діалог підтвердження відкрито | 1. Натиснути кнопку `Delete` (contained, color=error) | Діалог закривається; транзакція зникає зі списку | Видалення працює, транзакція зникає | 🔴 High |
| UI-16 | Пагінація — відображення та перехід | Авторизований; кількість транзакцій > 5 (`itemsPerPage`=`5`) | 1. Відкрити `/dashboard`<br>2. Переконатись що `totalPages > 1`<br>3. Клікнути на сторінку 2 | Компонент `<Pagination>` відображається; після кліку змінюється сторінка і список | Пагінація працює коректно | 🟠 High |

---

## 5 · ViewModal — редагування транзакції

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-17 | Відкриття `ViewModal` кліком на транзакцію | Авторизований, транзакції завантажені | 1. Клікнути на рядок транзакції<br>2. Перевірити модалку | `ViewModal` відкривається; дані підтягуються | Модалка відкривається з даними | 🟠 High |
| UI-18 | Збереження змін через `ViewModal` | `ViewModal` відкрито | 1. Змінити поле<br>2. Натиснути `Save` | Дані оновлюються, модалка закривається | Зміни зберігаються, модалка закривається | 🟠 High |

---

## 6 · Error handling

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-19 | `Alert` при помилці API | Є помилка в store | 1. Вимкнути інтернет<br>2. Відкрити `/dashboard` | Відображається `<Alert>` з помилкою; зникає через 5 сек | Алерт показується і зникає автоматично | 🔴 High |

---

## 7 · Адаптивність

| № | Назва | Передумови | Кроки | Очікуваний результат | Фактичний результат | Пріоритет |
|---|-------|-----------|-------|---------------------|---------------------|-----------|
| UI-20 | Перевірка повторного відкриття сторінки (reload) | Користувач авторизований | 1. Відкрити `/dashboard`<br>2. Оновити сторінку (F5) | Користувач залишається авторизованим; дані (баланс, транзакції) знову підвантажуються | Сесія зберігається, дані підтягуються повторно | 🟠 High |

---

